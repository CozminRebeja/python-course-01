sampleStr = "easyjob"

strLength = len(sampleStr)
middle = strLength // 2

middleThree = sampleStr[middle - 1] + sampleStr[middle] +  sampleStr[middle + 1]

print(f"Middle three chars are: {middleThree}\n")